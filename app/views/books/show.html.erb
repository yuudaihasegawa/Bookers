
<% if flash[:notice] %>
  <div class="flash">
  	<%= flash[:notice] %>
  </div>
<% end %>

<strong>Title:</strong>
<%= @book.title %>
<strong>Body:</strong>
<%= @book.body %>

<%= link_to "Edit", edit_book_path %>
<%= link_to "Back", books_path %>